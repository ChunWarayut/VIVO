@model IEnumerable<VIVO.Models.ProductType>

@{
    ViewBag.Title = "Index";
}

<h2>ระบบจัดการข้อมูล</h2>

<p>
    @Html.ActionLink("เพิ่มข้อมูล", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ProType_Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ProType_Name)
        </td>
        <td>
            @Html.ActionLink(" ", "Edit", new { id = item.ProType_Id }, new { @class = "fas fa-edit" }) |
            @Html.ActionLink(" ", "Details", new { id = item.ProType_Id }, new { @class = "fas fa-file" }) |
            @Html.ActionLink(" ", "Delete", new { id = item.ProType_Id }, new { @class = "fas fa-trash-alt" })
        </td>
    </tr>
}

</table>
