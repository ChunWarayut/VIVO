@model IEnumerable<VIVO.Models.Product>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Pro_Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Pro_Details)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Pro_Price)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Pro_Color)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Pro_Picture)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ProductType.ProType_Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Pro_Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Pro_Details)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Pro_Price)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Pro_Color)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Pro_Picture)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ProductType.ProType_Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Pro_Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Pro_Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Pro_Id })
        </td>
    </tr>
}

</table>
